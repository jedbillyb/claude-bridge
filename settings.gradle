plugins {
    // Lets Gradle auto-download a JDK 21 toolchain even if the system JDK differs.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'ClaudeBridge'
